name: Deploy to production

on: 
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  release-version:
    runs-on: ubuntu-latest
    steps:
      - name: Relase
        run: echo "ini relase ke $GITHUB_REF"
